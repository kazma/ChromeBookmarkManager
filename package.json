{
  "name": "bookmarkz-extension",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "build": "node esbuild.js",
    "watch": "node esbuild.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.9",
    "typescript": "^5.5.4"
  }
}
